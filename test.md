# AstrBot 插件「每日单词」开发计划

> 版本：v1.0.0 | 作者：你的名字 | 基于 AstrBot v4.x

---

## 一、需求确认（用户选型）

| 序号 | 选型项 | 用户选择 | 说明 |
|:---:|:---|:---|:---|
| 1 | 单词展示方式 | **B - Pillow 手绘卡片** | 轻量、可控、离线可用 |
| 2 | 复习抽词算法 | **A - 完全随机** | 简单公平，代码量少 |
| 3 | 打卡/推送机制 | **可配置开关 + 指定QQ号推送** | 灵活，用户自主控制 |
| 4 | 数据存储 | **B - SQLite** | 支持多用户，查询高效 |
| 5 | 扩展功能 | **④ 学习排行榜（群内）** | 群成员互相监督激励 |
| 6 | 使用场景 | **C - 私聊+群聊都支持** | 兼容两种交互方式 |

---

## 二、目录结构

```
astrbot_plugin_dailyword/
├── metadata.yaml              # 插件元数据
├── main.py                    # 主入口：指令注册、事件处理
├── requirements.txt           # 依赖：Pillow, aiosqlite
├── config.yaml                # 用户配置模板（推送开关、QQ号、目标数）
│
├── database/
│   ├── __init__.py
│   └── db.py                  # SQLite 封装：用户表、单词表、学习记录表
│
├── wordbank/
│   ├── __init__.py
│   ├── cet4_words.json        # 四级词库（约4500词）
│   └── loader.py              # 词库加载、抽词逻辑
│
├── renderer/
│   ├── __init__.py
│   └── card.py                # Pillow 卡片渲染：单词卡、排行榜卡
│
├── tasks/
│   ├── __init__.py
│   └── scheduler.py           # 定时任务：每日推送、打卡检查
│
└── assets/
    ├── fonts/                 # 中文字体（思源黑体等）
    └── bg/                    # 卡片背景图（可选）
```

---

## 三、数据表设计（SQLite）

### 3.1 用户表 `users`

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `user_id` | TEXT PRIMARY KEY | 平台标识，如 `qq_123456789` |
| `platform` | TEXT | 平台：`qq` / `wechat` / `telegram` |
| `native_id` | TEXT | 原平台ID |
| `streak_days` | INTEGER DEFAULT 0 | 连续打卡天数 |
| `last_checkin` | TEXT | 最后打卡日期 `YYYY-MM-DD` |
| `total_learned` | INTEGER DEFAULT 0 | 累计学习单词数 |
| `daily_target` | INTEGER DEFAULT 10 | 每日目标单词数 |
| `created_at` | TEXT | 注册时间 |

### 3.2 学习记录表 `learning_records`

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT | 自增ID |
| `user_id` | TEXT | 关联 users |
| `word` | TEXT | 单词 |
| `first_learned` | TEXT | 首次学习日期 |
| `last_review` | TEXT | 最后复习日期 |
| `review_count` | INTEGER DEFAULT 0 | 复习次数 |
| `proficiency` | REAL DEFAULT 0.0 | 熟练度 0.0~1.0 |

### 3.3 词库表 `word_bank`

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `word` | TEXT PRIMARY KEY | 单词 |
| `phonetic` | TEXT | 音标 |
| `meaning` | TEXT | 中文释义 |
| `example` | TEXT | 例句 |
| `category` | TEXT | 分类：`cet4` / `cet6` / `ielts` 等 |
| `frequency` | INTEGER | 词频权重（越高越常见）|

---

## 四、指令清单

| 指令 | 参数 | 功能 | 场景 |
|:---|:---|:---|:---|
| `/今日单词` | 无 | 从未学词库随机抽1个，自动打卡，发卡片 | 私聊/群聊 |
| `/复习单词` | 无 | 从已学词库随机抽1个复习，更新复习记录 | 私聊/群聊 |
| `/我的进度` | 无 | 发文字/卡片展示打卡天数、累计学习、连续天数 | 私聊/群聊 |
| `/单词本` | `[数量]` | 列出最近学过的N个单词（默认10个） | 私聊/群聊 |
| `/排行榜` | 无 | 生成群内学习排行榜图片（仅限群聊） | 群聊 |
| `/设置目标` | `<数字>` | 设置每日学习目标数量 | 私聊/群聊 |
| `/推送设置` | `on/off/qq号` | 开启/关闭自动推送，或设置推送QQ | 私聊（管理员） |

---

## 五、核心流程图

### 5.1 今日单词流程

```
用户发送 /今日单词
    │
    ▼
┌─────────────────┐
│ 检查用户是否存在 │──否──→ 创建用户记录
│   (users表)     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 检查今日是否已打卡│──是──→ 提示"今日已学X词，明日再来"
│ (last_checkin)  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 查询已学单词列表 │
│(learning_records)│
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 从word_bank排除已学│──空──→ 提示"恭喜！你已学完所有单词"
│   随机抽取1个     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 写入学习记录     │
│ 更新用户打卡数据 │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Pillow渲染单词卡片│
│ (单词+音标+释义+例句+打卡信息) │
└─────────────────┘
    │
    ▼
发送图片结果给用户/群
```

### 5.2 复习单词流程

```
用户发送 /复习单词
    │
    ▼
┌─────────────────┐
│ 查询用户已学列表 │──空──→ 提示"你还没有学过单词，先发送 /今日单词"
│(review_count>0) │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 完全随机抽取1个  │
│ 已学过的单词     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 更新复习记录     │
│ review_count+1   │
│ last_review=今天 │
└─────────────────┘
    │
    ▼
渲染复习卡片（标注"复习第N次"）
    │
    ▼
发送图片结果
```

### 5.3 排行榜流程（群聊）

```
用户发送 /排行榜
    │
    ▼
┌─────────────────┐
│ 检查是否为群聊   │──否──→ 提示"排行榜仅在群聊中可用"
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 查询群内所有用户 │
│ 按 total_learned │
│ 或 streak_days 排序│
└─────────────────┘
    │
    ▼
渲染排行榜卡片（Top 10）
    │
    ▼
发送图片到群聊
```

---

## 六、Pillow 卡片设计规范

### 6.1 今日单词卡片（800×500）

```
┌────────────────────────────────────────┐
│  📖 每日单词                              │  ← 标题栏，背景色 #4A90D9
├────────────────────────────────────────┤
│                                        │
│   abandon                              │  ← 单词，字号 48px，加粗
│   /əˈbændən/                           │  ← 音标，字号 24px，灰色
│                                        │
│   v. 放弃，抛弃                          │  ← 释义，字号 28px
│                                        │
│   💡 "He abandoned his car in the        │  ← 例句，字号 22px，斜体
│      snow."                             │
│                                        │
├────────────────────────────────────────┤
│  🔥 连续 5 天  │  📚 累计 42 词  │  🎯 1/10 │  ← 底部统计栏
└────────────────────────────────────────┘
```

### 6.2 排行榜卡片（800×600）

```
┌────────────────────────────────────────┐
│  🏆 群内学习排行榜                         │
├────────────────────────────────────────┤
│  #1  小明        📚 128词   🔥 12天      │
│  #2  小红        📚 96词    🔥 8天       │  ← 每行一个用户
│  #3  你          📚 42词    🔥 5天  ←你   │  ← 高亮当前用户
│  ...                                   │
│                                        │
│  ─── 共 N 人参与学习 ───                 │
└────────────────────────────────────────┘
```

---

## 七、定时推送任务设计

### 7.1 配置项（config.yaml）

```yaml
# 自动推送设置
daily_push:
  enabled: true              # 总开关
  push_time: "08:00"         # 推送时间（24小时制）
  target_qq: "123456789"     # 推送目标QQ号（私聊）
  # 群聊推送用群号，私聊用QQ号
  target_type: "private"       # private / group
  target_id: "123456789"     # 具体ID

# 每日目标
daily_target: 10

# 词库设置
word_bank: "cet4"            # cet4 / cet6 / ielts / toefl
```

### 7.2 推送逻辑

```python
# scheduler.py 伪代码
async def daily_push_task():
    config = load_config()
    if not config["daily_push"]["enabled"]:
        return

    # 获取目标用户
    target = config["daily_push"]["target_id"]
    target_type = config["daily_push"]["target_type"]

    # 检查用户今日是否已打卡
    user = await db.get_user(target)
    if user and user["last_checkin"] == today:
        return  # 已学，不打扰

    # 自动抽词并发送
    word = await pick_new_word(target)
    card = render_word_card(word, user)

    if target_type == "private":
        await send_private_message(target, card)
    else:
        await send_group_message(target, card)
```

---

## 八、开发里程碑

| 阶段 | 内容 | 预计时间 |
|:---|:---|:---:|
| **M1** | 搭建骨架：`metadata.yaml` + `main.py` + 指令注册 | 30min |
| **M2** | 实现 SQLite 数据库层：`database/db.py` | 1h |
| **M3** | 导入词库 + 抽词逻辑：`wordbank/loader.py` | 30min |
| **M4** | Pillow 卡片渲染：`renderer/card.py` | 2h |
| **M5** | 实现 `/今日单词` + `/复习单词` + `/我的进度` | 1h |
| **M6** | 实现 `/排行榜` + 群聊适配 | 1.5h |
| **M7** | 定时推送任务：`tasks/scheduler.py` | 1h |
| **M8** | 配置系统 + 测试 + 打包 | 1h |
| | **总计** | **约 8.5h** |

---

## 九、依赖清单（requirements.txt）

```
Pillow>=10.0.0
aiosqlite>=0.19.0
```

> 注：`aiosqlite` 是 SQLite 的异步封装，适配 AstrBot 的异步架构。

---

## 十、风险与备选方案

| 风险 | 影响 | 备选方案 |
|:---|:---|:---|
| Pillow 中文字体缺失 | 卡片乱码 | 打包时内置思源黑体字体文件 |
| 词库 JSON 过大 | 启动加载慢 | 首次启动导入 SQLite，后续查表 |
| 定时任务与 AstrBot 调度冲突 | 推送不准时 | 用 AstrBot 的 `on_scheduled` 事件 |
| 群内用户隐私顾虑 | 排行榜泄露学习进度 | 增加 `/隐私设置` 关闭排行榜显示 |

---

## 十一、后续迭代方向

1. **v1.1**：增加多词库切换（`/切换词库 cet6`）
2. **v1.2**：增加单词发音（TTS）
3. **v1.3**：增加拼写测试模式
4. **v1.4**：艾宾浩斯复习算法升级（选型②→④）
5. **v1.5**：生词本导出 PDF

---

> 确认无误后，我将按此计划输出完整可运行的代码。